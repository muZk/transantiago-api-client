const agencies = require('./Agencies')
const bipSpots = require('./BipSpots')
const buses = require('./Buses')
const map = require('./Map')
const routes = require('./Routes')
const stops = require('./Stops')

module.exports = {
  agencies,
  bipSpots,
  buses,
  map,
  routes,
  stops
}
